apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "temporal-scaling-demo.fullname" . }}-main-worker-headless
  labels:
    {{- include "temporal-scaling-demo.labels" (list . "main-worker") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "temporal-scaling-demo.selectorLabels" (list . "main-worker") | nindent 6 }}
  endpoints:
    - port: metrics
      interval: 10s
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "temporal-scaling-demo.fullname" . }}-billing-worker-headless
  labels:
    {{- include "temporal-scaling-demo.labels" (list . "billing-worker") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "temporal-scaling-demo.selectorLabels" (list . "billing-worker") | nindent 6 }}
  endpoints:
    - port: metrics
      interval: 10s 